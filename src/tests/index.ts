import type { TestSuite } from "@elizaos/core";

// Currently no E2E test suites are defined
// Unit tests are run via vitest directly

export const testSuites: TestSuite[] = [
  // E2E test suites will be added here when created
];

export default testSuites; 